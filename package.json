{
  "name": "trustortrap",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c green,cyan \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix server run build || true && npm --prefix client run build",
    "start": "npm --prefix server start & npm --prefix client start"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
