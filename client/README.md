## Client

This folder contains the Next.js frontend for TrustOrTrap.

Useful commands:

```bash
npm run dev
npm run build
npm run start
```

The frontend expects `NEXT_PUBLIC_API_BASE` to point at the Express API.
For the full local setup and seed steps, use the repository root `README.md`.
